name:  Configuration ansible.cfg 

on:
  workflow_dispatch:

jobs:
  generate-ansible-cfg:
    runs-on: [ ansible-master ]

    steps:


      - name: Generate ansible.cfg
        run: |
          sudo -u rommel bash -c 'cat > /home/rommel/ansible/ansible.cfg' << 'EOF'
          [defaults]
          inventory      = /home/rommel/ansible/inventory.yaml
          roles_path    = /home/rommel/ansible/roles
          host_key_checking = False
          remote_user = rommel
          private_key_file = /home/rommel/.ssh/id_rsa
          log_path = /home/rommel/ansible/ansible.log
          forks = 5
          gathering = smart
          fact_caching = jsonfile
          fact_caching_connection = /home/rommel/ansible/facts_cache
          

          [privilege_escalation]
          become = True
          become_method = sudo
          become_user = root
          become_ask_pass = False

          [ssh_connection]
          ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o ConnectTimeout=10
          control_path = /home/rommel/.ssh/id_rsa-%%r@%%h:%%p
          EOF
          

      - name: Set proper permissions
        run: |
          sudo chown rommel:rommel /home/rommel/ansible/ansible.cfg
          sudo chmod 644 /home/rommel/ansible/ansible.cfg

      - name: Verify configuration
        run: |
          echo "Ansible configuration:"
          sudo -u rommel ls -la /home/rommel/ansible/ansible.cfg
          echo -e "\nContents:"
          sudo -u rommel cat /home/rommel/ansible/ansible.cfg
